[build]
  command = "npm install && hugo"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.128.0"
  HUGO_ENABLEGITINFO = "true"
  NODE_VERSION = "18"
  POSTCSS_VERSION = "8.4.0"

[context.production.environment]
  HUGO_ENV = "production"
